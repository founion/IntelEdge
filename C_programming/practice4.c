#include <stdio.h>

int main()
{
	double width = 3, height = 5;
	double area = width * height / 2;
	printf("밑변이 %.2lfcm이고, 높이가 %.2lfcm인 삼각형의 넓이 = %lfcm^2\n", width, height, area);


	return 0;
}